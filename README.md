# YouTube Video Downloader

A simple YouTube video downloader using Flask and yt-dlp.

## Setup

1. Install dependencies:
   ```bash
   pip install -r requirements.txt
   ```

2. Run the app:
   ```bash
   python app.py
   ```

3. Open in browser: `http://127.0.0.1:5000/`
